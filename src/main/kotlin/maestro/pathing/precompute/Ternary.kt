package maestro.pathing.precompute

/**
 * A three-valued logic enum representing yes, maybe, or no states.
 *
 * Used in precomputation to represent uncertain or partially determined conditions.
 */
enum class Ternary {
    YES,
    MAYBE,
    NO,
}
